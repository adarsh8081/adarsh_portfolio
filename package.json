{
  "name": "myportfolio",
  "version": "1.0.0",
  "description": "Full-stack Portfolio with Express API and Next.js Frontend",
  "scripts": {
    "install": "cd server && npm install && cd ../web && npm install",
    "build": "cd server && npm run build && cd ../web && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "build:frontend": "cd web && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build",
    "prisma:generate": "cd server && npm run prisma:generate",
    "vercel-build": "npm run build:server && npm run build:frontend && npm run prisma:generate"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "private": true
}
